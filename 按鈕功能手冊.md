# 擎添耳溫槍探頭套檢測系統 — 按鈕功能手冊

> 此文件供現場操作員與系統使用單位參考。
> 「Master 限定」表示僅在 Master 模式下顯示；Slave 端看不到。

---

## 1. 主畫面 — 頂部 Banner

### 1.1 設定齒輪 ⚙️
| 項目 | 內容 |
|---|---|
| 位置 | 第一列右上角 |
| 功能 | 開啟 / 關閉右側設定抽屜 |
| 影響 PLC | 無 |
| 影響檔案 | 無 |
| 影響 UI | 顯示或隱藏設定面板 |

---

### 1.2 SAVE （批號）
| 項目 | 內容 |
|---|---|
| 位置 | 第二列「批號:」輸入框右側的藍色磁碟圖示 |
| 功能 | 把輸入框內容套用為目前批號 |
| 影響 PLC | 無 |
| 影響檔案 | `config.json` 寫入 `batch_no` 欄位；之後每筆量測 log 列首會帶此批號 |
| 影響 UI | 輸入框邊框變色，提示「批號已套用」 |
| 限制 | 限英數字、底線、連字號 (`-`)；空白時不更新 |
| 注意 | 改了批號**沒有按 SAVE** → 滑鼠移開 0.15 秒後自動還原原值 |

---

### 1.3 計數歸零（Master 限定）
| 項目 | 內容 |
|---|---|
| 位置 | 第二列右側橘色按鈕 |
| 功能 | 把 PLC 累積的 OK / NG 計數全部清為 0 |
| 影響 PLC | **D517~D528（OK 計數 1~12）寫 0**、**D529~D540（NG 計數 1~12）寫 0** |
| 影響檔案 | `config.json` 的 `last_reset_date` 更新為今天日期 |
| 影響 UI | 各通道 OK/NG 欄位顯示 0、頂部「TOTAL OK/TOTAL NG」歸 0 |
| 確認對話框 | 是（「將清除 PLC 與畫面上所有 OK/NG 計數，此動作無法復原」） |
| 失敗處理 | PLC 寫入失敗 → 顯示警告「PLC 未清，連線恢復後請再按一次」、`last_reset_date` 不會更新 |
| 注意 | 按過此按鈕後，**同一天重啟程式不會再次自動歸零**（防呆機制）|

---

### 1.4 異常復歸（Master 限定）
| 項目 | 內容 |
|---|---|
| 位置 | 第二列右側琥珀色按鈕 |
| 功能 | 解除 PLC 異常旗標、清除 HMI 內部異常狀態與警報橫幅 |
| 影響 PLC | **D513（異常 bits）整個寫 0** |
| 影響檔案 | 無 |
| 影響 UI | 各通道無套計數歸 0、各通道 highlight 移除、警報橫幅關閉 |
| 不會影響 | PLC OK / NG 計數（D517~D540）、批號、量測流程 |
| 確認對話框 | 否 |

---

### 1.5 流程解卡（Master 限定）
| 項目 | 內容 |
|---|---|
| 位置 | 第二列右側紅色按鈕 |
| 功能 | 強制清除量測 / 空槍觸發訊號，重置 HMI 量測狀態機 |
| 影響 PLC | **D500（量測觸發）寫 0**、**D515（空槍觸發）寫 0** |
| 影響檔案 | 無 |
| 影響 UI | 各通道空槍 / 溫度 / 誤差顯示清 0、燈號變灰、狀態文字變「WAIT」 |
| 確認對話框 | 是 |
| 使用時機 | 流程明顯卡住、量測觸發超過 15 秒沒進展時。**正常情況不應使用** |

---

## 2. 警報區

### 2.1 確認（紅色橫幅內）
| 項目 | 內容 |
|---|---|
| 位置 | 紅色警報橫幅出現時，右側白色按鈕 |
| 功能 | 停止警報閃爍、隱藏紅色橫幅 |
| 影響 PLC | 無 |
| 影響檔案 | 無 |
| 影響 UI | 紅色警報橫幅消失 |
| 注意 | 此操作**不會解除實際異常**，只是不再閃。要真正解除請按「異常復歸」 |

---

## 3. Log 區

### 3.1 清空 Log
| 項目 | 內容 |
|---|---|
| 位置 | Log 顯示區下方 |
| 功能 | 清空畫面上的訊息歷史 |
| 影響 PLC | 無 |
| 影響檔案 | **不影響**本機 CSV log 檔案，僅清螢幕顯示 |
| 影響 UI | Log 區清空 |

---

## 4. 手動觸發區（Master 限定）

> 主要用於**模擬模式測試**或實機 PLC 沒接時手動驗證流程。

### 4.1 空槍量測
| 項目 | 內容 |
|---|---|
| 位置 | 「手動觸發」卡片內，青色按鈕 |
| 功能 | 模擬 PLC 發出空槍量測訊號 |
| 影響 PLC | **D515 寫 1**，啟動空槍量測流程 |

### 4.2 溫度量測
| 項目 | 內容 |
|---|---|
| 位置 | 「手動觸發」卡片內，橘色按鈕 |
| 功能 | 模擬 PLC 發出溫度量測訊號 |
| 影響 PLC | **D500 寫 1**，啟動溫度量測流程 |

---

## 5. 設定面板（齒輪打開後）

### 5.1 關閉 ×
| 項目 | 內容 |
|---|---|
| 功能 | 關閉設定面板 |
| 影響 | 僅 UI |

### 5.2 登入 / 登出
| 項目 | 內容 |
|---|---|
| 功能 | 解鎖 / 鎖回密碼保護的進階設定 |
| 密碼 | 內建 `36274806`，或 `config.json` 中的 `extra_password`（預設 `1234`） |
| 影響 PLC | 無 |
| 影響檔案 | 無 |
| 影響 UI | 顯示 / 隱藏密碼保護的區塊（系統設定、網路、PLC、模擬、遠端記錄等） |

### 5.3 選擇資料夾 📁（遠端記錄區內）
| 項目 | 內容 |
|---|---|
| 位置 | 「遠端記錄」展開後，Log 路徑、Alarm 路徑右側的資料夾圖示 |
| 功能 | 開啟系統選資料夾對話框，選定後路徑寫入輸入框 |
| 影響 PLC | 無 |
| 影響檔案 | **此時尚未寫入 `config.json`**，需按「儲存進階設定」或「更新資料」才落地 |

### 5.4 儲存進階設定
| 項目 | 內容 |
|---|---|
| 位置 | 設定面板最下方藍色按鈕 |
| 功能 | 把目前所有設定欄位寫入 `config.json` |
| 影響 PLC | 無 |
| 影響檔案 | `config.json` 全部欄位重寫 |
| 影響 UI | 顯示「進階設定已儲存」 |
| 重啟才生效 | 模擬模式切換、IP/Port 變更等需重啟才會完整套用 |

### 5.5 更新資料 (即時套用)
| 項目 | 內容 |
|---|---|
| 位置 | 設定面板最下方綠色按鈕 |
| 功能 | 儲存設定 + **即時套用**到執行中的管理器（不需重啟） |
| 即時生效項目 | 量測誤差上下限、空槍上下限、藍芽 MAC、PLC IP/Port、網路參數、模擬模式（屬性層級）|
| 影響檔案 | `config.json` 全部欄位重寫 |
| 影響 UI | 顯示「設定已即時套用」；模擬模式變更時提示「建議重啟程式以完全生效」 |

---

## 6. 對話框類按鈕

| 按鈕 | 出現位置 | 動作 |
|---|---|---|
| 取消 | 計數歸零、流程解卡、其他確認對話框 | 關閉對話框，不執行任何操作 |
| 確認歸零 | 計數歸零對話框 | 執行「計數歸零」（同 §1.3）|
| 確認解卡 | 流程解卡對話框 | 執行「流程解卡」（同 §1.5）|

---

## 7. 自動觸發機制（無按鈕但會發生）

### 7.1 開機跨日歸零
| 項目 | 內容 |
|---|---|
| 觸發 | 程式啟動且 PLC 連線成功，且 `last_reset_date` ≠ 今天 |
| 動作 | 自動執行「計數歸零」（寫 PLC D517~D540 = 0），更新 `last_reset_date` 為今天 |
| 不觸發條件 | 同一天重啟程式 → 沿用既有計數，console 印「[啟動] 與上次歸零同日」 |
| Log 訊息 | `[啟動] 跨日自動歸零 (上次: YYYY-MM-DD → 今日: YYYY-MM-DD)` |

### 7.2 遠端 Alarm 非同步寫入
| 項目 | 內容 |
|---|---|
| 觸發 | 任何異常事件（如藍芽斷線、溫度異常、流程超時）|
| 本機 | 同步寫入 `D:\logs\Alarm\alarm_YYYYMMDD.csv`（不阻塞） |
| 遠端 | 非同步寫入 `<remote_alarm_dir>/alarm_YYYYMMDD_<machine_name>.csv`（背景處理） |
| 失敗處理 | 指數退避重試（1, 2, 4, 8, 16, 32, 60 秒），**永不放棄**；console 印重試訊息 |

### 7.3 遠端 Cycle Log 非同步寫入
| 項目 | 內容 |
|---|---|
| 觸發 | 每次量測週期完成（D515 空槍 / D500 量測都會寫一次）|
| 本機 | 同步寫入 `<log_dir>/log_YYYYMMDD_<machine_name>.csv`（完整版，54 欄）|
| 遠端 | 非同步寫入 `<remote_log_dir>/summary_log_YYYYMMDD_<machine_name>.csv`（簡化版：批號 / 時間 / TOTAL OK / TOTAL NG）|
| 失敗處理 | 指數退避重試，佇列上限 100000 筆（約 83 小時緩衝）；超過時拒絕新筆但本機 log 不受影響 |

---

## 8. 重要原則速記

1. **本機 log 永遠是 source of truth**——遠端只是備份，遠端壞掉/離線完全不影響本機紀錄與量測流程
2. **計數歸零 ≠ 異常復歸**——前者清 PLC 計數，後者清異常旗標。功能不重疊
3. **流程解卡是緊急按鈕**——僅在卡住時用，平常不要按
4. **批號改了一定要按 SAVE**——否則 0.15 秒後自動還原
5. **設定面板修改後須按「儲存進階設定」或「更新資料」才會寫入 `config.json`**
6. **跨日歸零是自動的**——操作員不必每天上班手動歸零，但程式必須隔天才開機才會生效
